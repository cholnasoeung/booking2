# Copy to .env in the project root on the VPS (docker-compose reads this
# automatically). Only used for nginx/certbot domain substitution and
# certificate bootstrapping — app secrets live in .env.production instead.

DOMAIN=yourdomain.com
CERTBOT_EMAIL=you@example.com
